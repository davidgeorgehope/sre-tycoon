<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>SRE Tycoon — <%= yield(:title) || "Manage. Ship. Survive." %></title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Source+Code+Pro:wght@300;400;500;600;700&display=swap');

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #0a0a0a;
      --bg-card: #111111;
      --bg-hover: #1a1a1a;
      --border: #222;
      --text: #c0c0c0;
      --text-dim: #666;
      --text-bright: #e0e0e0;
      --green: #00ff41;
      --green-dim: #00aa2a;
      --amber: #ffb000;
      --red: #ff3333;
      --blue: #00aaff;
      --cyan: #00ffff;
      --purple: #aa55ff;
    }

    body {
      font-family: 'Source Code Pro', 'SF Mono', 'Fira Code', monospace;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      font-size: 14px;
      min-height: 100vh;
    }

    /* CRT scanline effect */
    body::after {
      content: '';
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: repeating-linear-gradient(
        0deg,
        transparent,
        transparent 2px,
        rgba(0, 0, 0, 0.08) 2px,
        rgba(0, 0, 0, 0.08) 4px
      );
      pointer-events: none;
      z-index: 9999;
    }

    /* Subtle glow on text */
    .glow { text-shadow: 0 0 10px currentColor; }

    a { color: var(--green); text-decoration: none; transition: color 0.2s; }
    a:hover { color: var(--amber); text-shadow: 0 0 8px var(--amber); }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 20px;
    }

    /* Header */
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 1px solid var(--border);
      margin-bottom: 30px;
    }

    .ascii-logo {
      color: var(--green);
      font-size: 10px;
      line-height: 1.2;
      white-space: pre;
      margin-bottom: 10px;
      text-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
    }

    .header-subtitle {
      color: var(--text-dim);
      font-size: 12px;
      margin-top: 5px;
    }

    .header-nav {
      margin-top: 15px;
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    .header-nav a {
      color: var(--text-dim);
      font-size: 12px;
      padding: 4px 12px;
      border: 1px solid var(--border);
      border-radius: 3px;
      transition: all 0.2s;
    }

    .header-nav a:hover {
      color: var(--green);
      border-color: var(--green);
      background: rgba(0, 255, 65, 0.05);
    }

    /* Flash messages */
    .flash {
      padding: 12px 16px;
      margin-bottom: 20px;
      border-left: 3px solid var(--green);
      background: rgba(0, 255, 65, 0.05);
      font-size: 13px;
    }

    .flash.alert {
      border-left-color: var(--red);
      background: rgba(255, 51, 51, 0.05);
      color: var(--red);
    }

    /* Cards */
    .card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .card-header {
      color: var(--amber);
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--border);
    }

    /* Buttons */
    .btn {
      display: inline-block;
      padding: 8px 16px;
      font-family: inherit;
      font-size: 13px;
      border: 1px solid var(--green);
      background: transparent;
      color: var(--green);
      cursor: pointer;
      border-radius: 3px;
      transition: all 0.2s;
      text-align: center;
    }

    .btn:hover {
      background: rgba(0, 255, 65, 0.1);
      text-shadow: 0 0 8px var(--green);
      box-shadow: 0 0 15px rgba(0, 255, 65, 0.1);
      color: var(--green);
    }

    .btn-amber {
      border-color: var(--amber);
      color: var(--amber);
    }

    .btn-amber:hover {
      background: rgba(255, 176, 0, 0.1);
      text-shadow: 0 0 8px var(--amber);
      box-shadow: 0 0 15px rgba(255, 176, 0, 0.1);
      color: var(--amber);
    }

    .btn-red {
      border-color: var(--red);
      color: var(--red);
    }

    .btn-red:hover {
      background: rgba(255, 51, 51, 0.1);
      text-shadow: 0 0 8px var(--red);
      color: var(--red);
    }

    .btn-lg {
      padding: 12px 24px;
      font-size: 15px;
    }

    .btn:disabled, .btn.disabled {
      opacity: 0.3;
      cursor: not-allowed;
      pointer-events: none;
    }

    /* Form elements */
    input[type="text"], select {
      font-family: inherit;
      font-size: 14px;
      padding: 10px 14px;
      background: var(--bg);
      border: 1px solid var(--border);
      color: var(--text-bright);
      border-radius: 3px;
      width: 100%;
      transition: border-color 0.2s;
    }

    input[type="text"]:focus, select:focus {
      outline: none;
      border-color: var(--green);
      box-shadow: 0 0 10px rgba(0, 255, 65, 0.1);
    }

    label {
      display: block;
      color: var(--amber);
      font-size: 12px;
      margin-bottom: 6px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    /* Grid */
    .grid { display: grid; gap: 20px; }
    .grid-2 { grid-template-columns: 1fr 1fr; }
    .grid-3 { grid-template-columns: 1fr 1fr 1fr; }
    .grid-4 { grid-template-columns: 1fr 1fr 1fr 1fr; }

    @media (max-width: 768px) {
      .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
      .ascii-logo { font-size: 7px; }
      .container { padding: 10px; }
    }

    @media (max-width: 1024px) and (min-width: 769px) {
      .grid-4 { grid-template-columns: 1fr 1fr; }
      .grid-3 { grid-template-columns: 1fr 1fr; }
    }

    /* Metrics */
    .metric {
      text-align: center;
      padding: 12px;
    }

    .metric-value {
      font-size: 24px;
      font-weight: 700;
      color: var(--green);
    }

    .metric-value.warning { color: var(--amber); }
    .metric-value.danger { color: var(--red); }

    .metric-label {
      font-size: 11px;
      color: var(--text-dim);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-top: 4px;
    }

    /* Progress bars */
    .progress-bar {
      height: 6px;
      background: var(--bg);
      border-radius: 3px;
      overflow: hidden;
      margin-top: 8px;
    }

    .progress-fill {
      height: 100%;
      border-radius: 3px;
      transition: width 0.5s ease;
    }

    .progress-fill.green { background: var(--green); }
    .progress-fill.amber { background: var(--amber); }
    .progress-fill.red { background: var(--red); }

    /* Log entries */
    .log-entry {
      padding: 8px 12px;
      border-left: 3px solid var(--border);
      margin-bottom: 8px;
      font-size: 12px;
      background: rgba(0, 0, 0, 0.3);
    }

    .log-entry.success { border-left-color: var(--green); }
    .log-entry.warning { border-left-color: var(--amber); }
    .log-entry.danger { border-left-color: var(--red); }
    .log-entry.info { border-left-color: var(--blue); }

    .log-icon { margin-right: 8px; }

    /* Action grid */
    .action-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 15px;
      transition: all 0.2s;
      cursor: pointer;
    }

    .action-card:hover {
      border-color: var(--green);
      background: var(--bg-hover);
      box-shadow: 0 0 20px rgba(0, 255, 65, 0.05);
    }

    .action-emoji { font-size: 24px; margin-bottom: 8px; }
    .action-name { color: var(--green); font-weight: 600; font-size: 13px; }
    .action-desc { color: var(--text-dim); font-size: 11px; margin-top: 4px; }

    /* Scenario cards */
    .scenario-card {
      background: var(--bg-card);
      border: 2px solid var(--border);
      border-radius: 4px;
      padding: 20px;
      cursor: pointer;
      transition: all 0.2s;
      text-align: center;
    }

    .scenario-card:hover, .scenario-card.selected {
      border-color: var(--green);
      box-shadow: 0 0 20px rgba(0, 255, 65, 0.1);
    }

    .scenario-card.selected {
      background: rgba(0, 255, 65, 0.05);
    }

    .scenario-emoji { font-size: 36px; margin-bottom: 10px; }
    .scenario-name { color: var(--amber); font-weight: 600; font-size: 14px; }
    .scenario-desc { color: var(--text-dim); font-size: 11px; margin-top: 8px; }

    /* Table */
    table {
      width: 100%;
      border-collapse: collapse;
    }

    th {
      color: var(--amber);
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 8px 12px;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }

    td {
      padding: 8px 12px;
      border-bottom: 1px solid rgba(34, 34, 34, 0.5);
      font-size: 13px;
    }

    tr:hover td { background: var(--bg-hover); }

    .rank-1 { color: var(--amber); font-weight: 700; }
    .rank-2 { color: var(--text-bright); font-weight: 600; }
    .rank-3 { color: #cd7f32; font-weight: 600; }

    /* Game over overlay */
    .game-over-overlay {
      text-align: center;
      padding: 40px 20px;
    }

    .game-over-title {
      font-size: 36px;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .game-over-win { color: var(--green); text-shadow: 0 0 30px rgba(0, 255, 65, 0.5); }
    .game-over-lose { color: var(--red); text-shadow: 0 0 30px rgba(255, 51, 51, 0.5); }

    .game-over-reason {
      color: var(--text-dim);
      font-size: 16px;
      margin-bottom: 30px;
    }

    .game-over-score {
      font-size: 48px;
      color: var(--amber);
      font-weight: 700;
      text-shadow: 0 0 20px rgba(255, 176, 0, 0.3);
    }

    .game-over-score-label {
      color: var(--text-dim);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    /* Sprint counter */
    .sprint-counter {
      display: inline-block;
      background: var(--bg-card);
      border: 1px solid var(--amber);
      color: var(--amber);
      padding: 4px 12px;
      border-radius: 3px;
      font-size: 12px;
      font-weight: 600;
    }

    /* Action points */
    .ap-dots {
      display: flex;
      gap: 6px;
      justify-content: center;
      margin: 10px 0;
    }

    .ap-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      border: 2px solid var(--green);
      transition: all 0.3s;
    }

    .ap-dot.filled {
      background: var(--green);
      box-shadow: 0 0 8px rgba(0, 255, 65, 0.5);
    }

    .ap-dot.empty {
      border-color: var(--border);
    }

    /* Blinking cursor */
    .cursor-blink::after {
      content: '█';
      animation: blink 1s infinite;
    }

    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }

    /* Footer */
    .footer {
      text-align: center;
      padding: 30px 0;
      color: var(--text-dim);
      font-size: 11px;
      border-top: 1px solid var(--border);
      margin-top: 40px;
    }

    .text-center { text-align: center; }
    .mb-20 { margin-bottom: 20px; }
    .mt-20 { margin-top: 20px; }
    .mt-10 { margin-top: 10px; }
  </style>
</head>
<body>
  <div class="container">
    <header class="header">
      <div class="ascii-logo">
 ███████╗██████╗ ███████╗  ████████╗██╗   ██╗ ██████╗ ██████╗  ██████╗ ███╗   ██╗
 ██╔════╝██╔══██╗██╔════╝  ╚══██╔══╝╚██╗ ██╔╝██╔════╝██╔═══██╗██╔═══██╗████╗  ██║
 ███████╗██████╔╝█████╗       ██║    ╚████╔╝ ██║     ██║   ██║██║   ██║██╔██╗ ██║
 ╚════██║██╔══██╗██╔══╝       ██║     ╚██╔╝  ██║     ██║   ██║██║   ██║██║╚██╗██║
 ███████║██║  ██║███████╗     ██║      ██║   ╚██████╗╚██████╔╝╚██████╔╝██║ ╚████║
 ╚══════╝╚═╝  ╚═╝╚══════╝     ╚═╝      ╚═╝    ╚═════╝ ╚═════╝  ╚═════╝ ╚═╝  ╚═══╝
      </div>
      <div class="header-subtitle">VP of Engineering Simulator • Manage. Ship. Survive.</div>
      <nav class="header-nav">
        <a href="<%= root_path %>">⌂ HOME</a>
        <a href="<%= new_game_path %>">+ NEW GAME</a>
        <a href="<%= leaderboard_games_path %>">⏱ LEADERBOARD</a>
      </nav>
    </header>

    <% if flash[:notice] %>
      <div class="flash"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert] %>
      <div class="flash alert"><%= flash[:alert] %></div>
    <% end %>

    <%= yield %>

    <footer class="footer">
      SRE Tycoon v1.0 • "It's not a bug, it's an unplanned feature" • Built with Rails <%= Rails::VERSION::STRING %>
    </footer>
  </div>
</body>
</html>
